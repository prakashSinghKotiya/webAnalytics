# Copy to `.env.local` and point these at the backend.
# The backend serves its routes at the root (no `/api` prefix) and listens on 5000.

VITE_API_URL=http://localhost:5000
VITE_SOCKET_URL=http://localhost:5000
